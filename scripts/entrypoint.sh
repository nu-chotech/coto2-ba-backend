#!/bin/sh
# ============================================================
# entrypoint.sh — コンテナ起動時エントリポイント
# ============================================================
# 1. モデルファイルの存在確認 / ダウンロード
# 2. uvicorn でアプリケーションを起動
# ============================================================
set -eu

echo "=========================================="
echo "  Coto2-Ba Backend — 起動中..."
echo "=========================================="

# モデルのダウンロード（必要な場合のみ）
/app/scripts/download_model.sh

echo "[app] サーバーを起動します..."
exec uvicorn app.main:app --host 0.0.0.0 --port 8000
